extends HitscanTrail

# Make lightning coil around trail later

func initialize(position_one : Vector3, target_position : Vector3) -> void:
	super(position_one, target_position)
	
	#var horizontal_trail_size = Vector3(0.0, 0.0, trail_height)
	
	#$HorizontalTrail.mesh = draw_mesh(position_one, target_position, horizontal_trail_size)
